# what should the container and image be called
# <container name>  <image name> 
name: nginx_herolab nginx/herolab

# what ports should be exposed
ports: 9500:9500

# what hostnames to ceate in the container
# the <container name> <domain name>
# links: aspnet_herolab:mysql.local 

mounts: /stacks:/stacks /stacker:/stacker

#working directory in the container
working_dir: /stacks/OpenResty
